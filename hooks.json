{
  "version": 1,
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "bash": "plugin_root=\"${PLUGIN_ROOT:-}\"; if [ -z \"$plugin_root\" ]; then printf '%s\\n' 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.' >&2; exit 1; fi; unset NODE_OPTIONS COPILOT_GITHUB_TOKEN GH_TOKEN GITHUB_TOKEN; node \"$plugin_root/src/hook-launcher.mjs\" SessionStart",
        "powershell": "$pluginRoot=$env:PLUGIN_ROOT; if ([string]::IsNullOrWhiteSpace($pluginRoot)) { Write-Error 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.'; exit 1 }; $env:NODE_OPTIONS=$null; $env:COPILOT_GITHUB_TOKEN=$null; $env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null; node (Join-Path $pluginRoot 'src/hook-launcher.mjs') SessionStart",
        "timeoutSec": 5
      }
    ],
    "PreToolUse": [
      {
        "type": "command",
        "matcher": ".*",
        "bash": "plugin_root=\"${PLUGIN_ROOT:-}\"; if [ -z \"$plugin_root\" ]; then printf '%s\\n' 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.' >&2; exit 1; fi; unset NODE_OPTIONS COPILOT_GITHUB_TOKEN GH_TOKEN GITHUB_TOKEN; node \"$plugin_root/src/hook-launcher.mjs\" PreToolUse",
        "powershell": "$pluginRoot=$env:PLUGIN_ROOT; if ([string]::IsNullOrWhiteSpace($pluginRoot)) { Write-Error 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.'; exit 1 }; $env:NODE_OPTIONS=$null; $env:COPILOT_GITHUB_TOKEN=$null; $env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null; node (Join-Path $pluginRoot 'src/hook-launcher.mjs') PreToolUse",
        "timeoutSec": 5
      }
    ],
    "PostToolUse": [
      {
        "type": "command",
        "bash": "plugin_root=\"${PLUGIN_ROOT:-}\"; if [ -z \"$plugin_root\" ]; then printf '%s\\n' 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.' >&2; exit 1; fi; unset NODE_OPTIONS COPILOT_GITHUB_TOKEN GH_TOKEN GITHUB_TOKEN; node \"$plugin_root/src/hook-launcher.mjs\" PostToolUse",
        "powershell": "$pluginRoot=$env:PLUGIN_ROOT; if ([string]::IsNullOrWhiteSpace($pluginRoot)) { Write-Error 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.'; exit 1 }; $env:NODE_OPTIONS=$null; $env:COPILOT_GITHUB_TOKEN=$null; $env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null; node (Join-Path $pluginRoot 'src/hook-launcher.mjs') PostToolUse",
        "timeoutSec": 5
      }
    ],
    "PostToolUseFailure": [
      {
        "type": "command",
        "bash": "plugin_root=\"${PLUGIN_ROOT:-}\"; if [ -z \"$plugin_root\" ]; then printf '%s\\n' 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.' >&2; exit 1; fi; unset NODE_OPTIONS COPILOT_GITHUB_TOKEN GH_TOKEN GITHUB_TOKEN; node \"$plugin_root/src/hook-launcher.mjs\" PostToolUseFailure",
        "powershell": "$pluginRoot=$env:PLUGIN_ROOT; if ([string]::IsNullOrWhiteSpace($pluginRoot)) { Write-Error 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.'; exit 1 }; $env:NODE_OPTIONS=$null; $env:COPILOT_GITHUB_TOKEN=$null; $env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null; node (Join-Path $pluginRoot 'src/hook-launcher.mjs') PostToolUseFailure",
        "timeoutSec": 5
      }
    ],
    "PreCompact": [
      {
        "type": "command",
        "bash": "plugin_root=\"${PLUGIN_ROOT:-}\"; if [ -z \"$plugin_root\" ]; then printf '%s\\n' 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.' >&2; exit 1; fi; unset NODE_OPTIONS COPILOT_GITHUB_TOKEN GH_TOKEN GITHUB_TOKEN; node \"$plugin_root/src/hook-launcher.mjs\" PreCompact",
        "powershell": "$pluginRoot=$env:PLUGIN_ROOT; if ([string]::IsNullOrWhiteSpace($pluginRoot)) { Write-Error 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.'; exit 1 }; $env:NODE_OPTIONS=$null; $env:COPILOT_GITHUB_TOKEN=$null; $env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null; node (Join-Path $pluginRoot 'src/hook-launcher.mjs') PreCompact",
        "timeoutSec": 5
      }
    ],
    "Stop": [
      {
        "type": "command",
        "bash": "plugin_root=\"${PLUGIN_ROOT:-}\"; if [ -z \"$plugin_root\" ]; then printf '%s\\n' 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.' >&2; exit 1; fi; unset NODE_OPTIONS COPILOT_GITHUB_TOKEN GH_TOKEN GITHUB_TOKEN; node \"$plugin_root/src/hook-launcher.mjs\" Stop",
        "powershell": "$pluginRoot=$env:PLUGIN_ROOT; if ([string]::IsNullOrWhiteSpace($pluginRoot)) { Write-Error 'Supervised Worker requires PLUGIN_ROOT; run node src/cli.mjs install for VS Code.'; exit 1 }; $env:NODE_OPTIONS=$null; $env:COPILOT_GITHUB_TOKEN=$null; $env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null; node (Join-Path $pluginRoot 'src/hook-launcher.mjs') Stop",
        "timeoutSec": 5
      }
    ]
  }
}